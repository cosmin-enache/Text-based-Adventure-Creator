/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */

const STORY_BOARD = querySelectorOrThrow("#story-board");
const EXPORT_BUTTON = querySelectorOrThrow("#" + ID_EXPORT_BUTTON);
const EXPORT_OUTPUT = querySelectorOrThrow("#" + ID_EXPORT_OUTPUT);
const IMPORT_AREA = querySelectorOrThrow("#" + ID_IMPORT_AREA);
const STORY_TEXT = querySelectorOrThrow("#" + ID_STORY_TEXT);
const CHOICES_LIST = querySelectorOrThrow("#" + ID_CHOICES_LIST);
